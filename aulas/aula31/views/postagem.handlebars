<div class="card">
    <div class="card-body">
        <h1>{{postagem.titulo}}</h1>
        <hr>
        <p>{{postagem.conteudo}}</p>
        <hr>
        {{categoria.nome}}
        {{postagem.data}}
    </div>
</div>